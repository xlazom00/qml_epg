#ifndef AUTOBUS_H
#define AUTOBUS_H

#include <QObject>

class Autobus : public QObject
{
    Q_OBJECT
public:
    explicit Autobus(QObject *parent = 0);

signals:

public slots:

};

#endif // AUTOBUS_H
